services:
  linklog:
    image: ghcr.io/korjavin/linklog:fb95399f38b2bf1499e57d4164c5573ccc7f76ad
    restart: unless-stopped
    environment:
      - TELEGRAM_BOT_TOKEN
      - TELEGRAM_ADMIN_CHAT_ID
      - OUTLINE_API_KEY
      - OUTLINE_BASE_URL
      - OUTLINE_COLLECTION_ID
      - LLM_API_KEY
      - LLM_BASE_URL
      - LLM_MODEL
      - SCHEDULE_DOC_ID
